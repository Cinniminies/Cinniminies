# Copiá este archivo como .env.local (que NO se commitea) y completá los valores.
# Solo para scripts locales (migracion/). Nunca pongas la service_role key en el front.
SUPABASE_URL=https://skysdjfxuykrufawhzvn.supabase.co
# Project Settings → API Keys → service_role (secret). Da acceso total: no la compartas.
SUPABASE_SERVICE_ROLE_KEY=
